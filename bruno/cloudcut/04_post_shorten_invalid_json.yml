info:
  name: 04_post_shorten_invalid_json
  type: http
  seq: 4

http:
  method: POST
  url: https://cloudcut.hermannleboss.workers.dev/api/shorten
  auth: inherit
  headers:
    Content-Type: application/json
  body:
    type: text
    data: |
      { invalid json }

runtime:
  scripts:
    - type: tests
      code: |-
        test("status should be 400", function() {
          expect(res.status).to.equal(400);
        });

        test("error should mention invalid json body", function() {
          expect(res.body.error).to.equal("Invalid JSON body");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
